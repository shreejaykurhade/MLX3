{
  "name": "mlx3-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "MLX3 smart contracts — ProviderRegistry & ExecutionAttestation on Monad testnet",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:monad": "hardhat run scripts/deploy.ts --network monadTestnet",
    "seed:monad": "hardhat run scripts/seed-providers.ts --network monadTestnet",
    "verify:monad": "hardhat verify --network monadTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/chai": "^4.3.16",
    "@types/mocha": "^10.0.7",
    "@types/node": "^20.14.0",
    "chai": "^4.4.1",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
